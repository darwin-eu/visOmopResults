# Pre-defined styles are available for tables

This function provides a list of pre-defined styles for tables that can
then be used in the `style` argument of the table functions.

## Usage

``` r
tableStyle()
```

## Value

A character vector indicating the style names.

## Examples

``` r
tableStyle()
#> [1] "darwin"  "default"
```
