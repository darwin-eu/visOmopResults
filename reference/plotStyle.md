# Pre-defined styles are available for plots

This function provides a list of pre-defined styles for plots that can
then be used in the `style` argument of the plot functions.

## Usage

``` r
plotStyle()
```

## Value

A character vector indicating the style names.

## Examples

``` r
plotStyle()
#> [1] "darwin"  "default"
```
